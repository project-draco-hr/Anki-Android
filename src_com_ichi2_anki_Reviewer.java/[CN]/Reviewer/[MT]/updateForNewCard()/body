{
  updateScreenCounts();
  if (mShowProgressBars) {
    updateStatisticBars();
  }
  if (typeAnswer()) {
    mAnswerField.setText("");
  }
  if (mPrefWhiteboard && !mShowAnimations) {
    mWhiteboard.clear();
  }
}
