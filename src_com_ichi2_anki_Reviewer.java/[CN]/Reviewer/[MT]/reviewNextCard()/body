{
  updateCounts();
  mFlipCard.setChecked(false);
  if (prefWriteAnswers)   mAnswerField.setText("");
  if (prefWhiteboard)   mWhiteboard.clear();
  if (prefTimer) {
    mCardTimer.setBase(SystemClock.elapsedRealtime());
    mCardTimer.start();
  }
}
