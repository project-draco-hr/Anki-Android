{
  try {
    if (qconf) {
      return mQconf.getInt(name);
    }
 else {
      return mConf.getInt(name);
    }
  }
 catch (  JSONException e) {
    if (fallback != 0) {
      return 0;
    }
 else {
      throw new RuntimeException(e);
    }
  }
}
