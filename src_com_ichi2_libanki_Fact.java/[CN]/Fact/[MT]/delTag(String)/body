{
  List<String> other=new ArrayList<String>();
  for (  String t : mTags) {
    if (!t.toLowerCase().equals(tag.toLowerCase())) {
      other.add(t);
    }
  }
  mTags.clear();
  mTags.addAll(other);
}
