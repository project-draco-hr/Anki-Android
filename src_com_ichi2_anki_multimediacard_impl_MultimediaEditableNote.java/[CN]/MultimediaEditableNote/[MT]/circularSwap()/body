{
  if (mFields == null)   return;
  if (mFields.size() <= 1)   return;
  ArrayList<IField> newFields=new ArrayList<IField>();
  newFields.add(mFields.get(mFields.size() - 1));
  newFields.addAll(mFields);
  newFields.remove(mFields.size());
  mFields=newFields;
}
