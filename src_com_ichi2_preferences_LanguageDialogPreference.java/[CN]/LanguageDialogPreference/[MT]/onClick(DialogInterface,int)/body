{
  if (which == DialogInterface.BUTTON_POSITIVE) {
    if (MetaDB.resetDB(mContext)) {
      Toast successReport=Toast.makeText(this.getContext(),AnkiDroidApp.getAppResources().getString(R.string.reset_languages_confirmation),Toast.LENGTH_SHORT);
      successReport.show();
    }
  }
}
