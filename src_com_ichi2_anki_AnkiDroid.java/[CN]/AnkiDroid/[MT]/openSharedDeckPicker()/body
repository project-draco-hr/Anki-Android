{
  if (AnkiDroidApp.deck() != null && sdCardAvailable)   AnkiDroidApp.deck().closeDeck();
  deckLoaded=false;
  Intent intent=new Intent(AnkiDroid.this,SharedDeckPicker.class);
  startActivityForResult(intent,DOWNLOAD_SHARED_DECK);
}
