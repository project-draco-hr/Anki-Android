{
  if (AnkiDroidApp.getDeck() != null && sdCardAvailable)   AnkiDroidApp.getDeck().closeDeck();
  deckLoaded=false;
  Intent intent=new Intent(AnkiDroid.this,SharedDeckPicker.class);
  startActivityForResult(intent,GET_SHARED_DECK);
}
