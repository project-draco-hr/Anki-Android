{
  double series[]=new double[length];
  if (backwards) {
    for (int i=0; i < length; i++) {
      series[i]=i - length;
    }
  }
 else {
    for (int i=0; i < length; i++) {
      series[i]=i;
    }
  }
  return series;
}
