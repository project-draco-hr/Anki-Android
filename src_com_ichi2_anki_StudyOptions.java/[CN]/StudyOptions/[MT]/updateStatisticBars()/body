{
  int maxWidth=mBarsMax.getWidth();
  int maxHeight=mBarsMax.getHeight();
  Utils.updateProgressBars(this,mGlobalMatBar,mProgressMature,maxWidth,maxHeight,false);
  Utils.updateProgressBars(this,mGlobalBar,(mProgressAll == 1.0) ? 1.0 : mProgressAll - mProgressMature,maxWidth,maxHeight,false);
}
