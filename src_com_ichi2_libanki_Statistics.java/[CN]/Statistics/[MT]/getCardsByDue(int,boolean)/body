{
  double series[]=new double[length];
  for (int i=1; i < length; i++) {
    int count=0;
    if (cumulative) {
      series[i]=count + series[i - 1];
    }
 else {
      series[i]=count;
    }
  }
  return series;
}
