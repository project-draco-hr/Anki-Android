{
  updateCheckedItems();
  ArrayList<String> selecteds=new ArrayList<String>();
  for (int i=0; i < mItemList.size(); i++) {
    if (mCheckedItems[i])     selecteds.add(mItemList.get(i));
  }
  return selecteds;
}
