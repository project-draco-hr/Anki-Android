{
  if (atomType != Atom.AtomType.INVALID) {
    return expressionLeft.isVariable() || expressionRight.isVariable();
  }
 else   throw new ExpressionFormatException("Number is Invalid, cannot parse");
}
