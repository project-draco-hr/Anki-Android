{
  JSONArray media=new JSONArray();
  Cursor cursor=mDeck.getDB().getDatabase().rawQuery("SELECT id, filename, size, created, originalPath, description FROM media WHERE id IN " + Utils.ids2str(ids),null);
  while (cursor.moveToNext()) {
    JSONArray m=new JSONArray();
    m.put(cursor.getLong(0));
    m.put(cursor.getString(1));
    m.put(cursor.getInt(2));
    m.put(cursor.getDouble(3));
    m.put(cursor.getString(4));
    m.put(cursor.getString(5));
    media.put(m);
  }
  cursor.close();
  return media;
}
