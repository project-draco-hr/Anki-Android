{
  String str="(";
  if (ids != null) {
    int len=ids.size();
    for (int i=0; i < len; i++) {
      if (i == (len - 1)) {
        str+=ids.get(i);
      }
 else {
        str+=ids.get(i) + ",";
      }
    }
  }
  str+=")";
  return str;
}
