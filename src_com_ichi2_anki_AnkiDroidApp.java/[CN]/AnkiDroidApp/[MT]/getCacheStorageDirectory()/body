{
  File cache=new File(getStorageDirectory() + "/Android/data/" + AnkiDroidApp.getInstance().getPackageName()+ "."+ AnkiDroidApp.getAppName()+ "/cache/");
  if (!cache.exists()) {
    cache.mkdirs();
  }
  return cache.getAbsolutePath();
}
