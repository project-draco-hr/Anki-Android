{
  File cache=new File(String.format(Locale.US,"%s/Android/data/%s.%s/cache/",getStorageDirectory(),AnkiDroidApp.getInstance().getPackageName(),AnkiDroidApp.getAppName()));
  if (!cache.exists()) {
    cache.mkdirs();
  }
  return cache.getAbsolutePath();
}
