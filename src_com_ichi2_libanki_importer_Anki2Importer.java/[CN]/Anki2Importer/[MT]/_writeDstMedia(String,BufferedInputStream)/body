{
  try {
    Utils.writeToFile(is,mDstMediaDir + fname);
  }
 catch (  IOException e) {
    Log.e(AnkiDroidApp.TAG,String.format(Locale.US,"Anki2Importer._writeDstMedia: error copying file to %s (%s), ignoring and continuing.",fname,e.getMessage()));
  }
}
