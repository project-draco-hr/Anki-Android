{
  AnkiDb ankiDB=AnkiDatabaseManager.getDatabase(path);
  newCount=ankiDB.queryScalar(cardLimit("revActive","revInactive","SELECT count(*) FROM cards c WHERE type = 2 AND combinedDue < " + dueCutoff));
}
