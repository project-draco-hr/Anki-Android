{
  AnkiDb ankiDB=AnkiDatabaseManager.getDatabase(path);
  newCount=ankiDB.queryScalar(cardLimit("newActive","newInactive","SELECT count(*) FROM cards c WHERE type = 2 AND combinedDue < " + dueCutoff));
}
