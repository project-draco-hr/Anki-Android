{
  if (!mUndoEnabled) {
    return;
  }
  commitToDB();
  long end=latestUndoRow();
  while (mUndoStack.peek() == null) {
    mUndoStack.pop();
  }
  UndoRow row=mUndoStack.peek();
  row.mEnd=end;
  if (row.mStart == row.mEnd) {
    mUndoStack.pop();
  }
 else {
    mRedoStack.clear();
  }
}
