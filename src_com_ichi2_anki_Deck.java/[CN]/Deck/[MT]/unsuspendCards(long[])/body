{
  getDB().database.execSQL("UPDATE cards SET type = type + 3, priority = 0, " + "modified = " + String.format(ENGLISH_LOCALE,"%f",(double)(System.currentTimeMillis() / 1000.0)) + " WHERE type < 0 AND id IN "+ Utils.ids2str(ids));
  updatePriorities(ids);
  flushMod();
  reset();
}
