{
  AnkiDatabaseManager.getDatabase(mDeckPath).getDatabase().execSQL("UPDATE cards SET priority = " + Card.PRIORITY_NONE + ", modified = "+ String.format(Utils.ENGLISH_LOCALE,"%f",Utils.now())+ " WHERE id IN "+ Utils.ids2str(ids));
  updatePriorities(ids);
  rebuildCounts(false);
  flushMod();
}
