{
  AnkiDatabaseManager.getDatabase(mDeckPath).getDatabase().execSQL("UPDATE cards SET isDue = 0" + ", priority = " + Card.PRIORITY_SUSPENDED + ", modified = "+ String.format(ENGLISH_LOCALE,"%f",Utils.now())+ " WHERE id IN "+ Utils.ids2str(ids));
  rebuildCounts(false);
  flushMod();
}
