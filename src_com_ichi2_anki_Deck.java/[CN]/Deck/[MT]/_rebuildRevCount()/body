{
  String sql=String.format(ENGLISH_LOCALE,"SELECT count(*) FROM cards c WHERE type = 1 AND combinedDue < %f",dueCutoff);
  revCount=(int)getDB().queryScalar(cardLimit("revActive","revInactive",sql));
}
