{
  AnkiDb ankiDB=AnkiDatabaseManager.getDatabase(path);
  failedSoonCount=ankiDB.queryScalar(cardLimit("revActive","revInactive","SELECT count(*) FROM cards c WHERE type = 0 AND combinedDue < " + dueCutoff));
}
