{
  AnkiDb ankiDB=AnkiDatabaseManager.getDatabase(deckPath);
  failedSoonCount=(int)ankiDB.queryScalar(cardLimit("revActive","revInactive","SELECT count(*) FROM cards c WHERE type = 0 AND combinedDue < " + dueCutoff));
}
