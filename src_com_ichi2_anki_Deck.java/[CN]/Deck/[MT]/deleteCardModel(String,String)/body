{
  Log.i(TAG,"deleteCardModel, modelId = " + modelId + ", fieldModelId = "+ cardModelId);
  ArrayList<String> cardIds=getDB().queryColumn(String.class,"SELECT id FROM cards WHERE cardModelId = " + cardModelId,0);
  deleteCards(cardIds);
  getDB().database.execSQL("DELETE FROM cardModels WHERE id = " + cardModelId);
  getDB().database.execSQL("UPDATE models SET modified = " + String.format(ENGLISH_LOCALE,"%f",System.currentTimeMillis() / 1000.0) + " WHERE id = "+ modelId);
  flushMod();
}
