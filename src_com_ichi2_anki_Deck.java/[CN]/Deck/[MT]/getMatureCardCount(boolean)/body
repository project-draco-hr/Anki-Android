{
  String sql=String.format(Utils.ENGLISH_LOCALE,"SELECT count(*) from cards c WHERE interval >= %d",Card.MATURE_THRESHOLD);
  if (restrictToActive) {
    return (int)getDB().queryScalar(cardLimit("revActive","revInactive",sql));
  }
 else {
    return (int)getDB().queryScalar(sql);
  }
}
