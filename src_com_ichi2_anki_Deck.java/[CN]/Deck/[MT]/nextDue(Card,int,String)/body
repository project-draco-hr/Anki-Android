{
  double due;
  if (ease == 1) {
    if (oldState.equals("mature")) {
      due=delay1 * 86400.0;
    }
 else {
      due=0.0;
    }
  }
 else {
    due=card.interval * 86400.0;
  }
  return due + (System.currentTimeMillis() / 1000.0);
}
