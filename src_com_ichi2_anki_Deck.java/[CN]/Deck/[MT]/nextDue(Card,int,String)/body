{
  double due;
  if (ease == 1) {
    if (oldState.equals("mature")) {
      due=mDelay1;
    }
 else {
      due=mDelay0;
    }
  }
 else {
    due=card.getInterval() * 86400.0;
  }
  return (due + Utils.now());
}
