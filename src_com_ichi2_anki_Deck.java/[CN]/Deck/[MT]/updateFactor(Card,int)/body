{
  card.lastFactor=card.factor;
  if (card.reps == 0)   card.factor=this.averageFactor;
  if (cardIsBeingLearnt(card) && (ease == 0 || ease == 1 || ease == 2)) {
    if (card.successive != 0 && ease != 2)     card.factor-=0.20f;
  }
 else   if (ease == 0 || ease == 1)   card.factor-=0.20f;
 else   if (ease == 2)   card.factor-=0.15f;
 else   if (ease == 4)   card.factor+=0.10f;
  card.factor=Math.max(1.3f,card.factor);
}
