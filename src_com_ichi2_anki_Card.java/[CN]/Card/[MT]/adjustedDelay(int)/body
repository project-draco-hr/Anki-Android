{
  double dueCutoff=mDeck.getDueCutoff();
  if (isNew()) {
    return 0;
  }
  if (mDue <= dueCutoff) {
    return (dueCutoff - mDue) / 86400.0;
  }
 else {
    return (dueCutoff - mDue) / 86400.0;
  }
}
