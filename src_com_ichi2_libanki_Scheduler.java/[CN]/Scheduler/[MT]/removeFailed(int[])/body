{
  String extra="";
  if (ids != null) {
    extra=" AND id IN " + Utils.ids2str(ids);
  }
  mDb.getDatabase().execSQL("UPDATE cards SET " + "due = edue, queue = 2, mod = " + Utils.intNow() + " WHERE queue = 1 AND type = 2 "+ extra);
}
