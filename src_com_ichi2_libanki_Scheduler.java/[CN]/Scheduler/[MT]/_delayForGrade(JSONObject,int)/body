{
  int delay;
  JSONArray ja;
  try {
    ja=conf.getJSONArray("delays");
    int len=ja.length();
    if (grade < len) {
      delay=ja.getInt(grade);
    }
 else {
      delay=ja.getInt(len);
    }
    return delay * 60;
  }
 catch (  JSONException e) {
    throw new RuntimeException(e);
  }
}
