{
  Timber.d("onResume()");
  super.onResume();
  if (colIsOpen()) {
    updateDeckList();
    hideProgressBar();
  }
  setTitle(getResources().getString(R.string.app_name));
  sIsWholeCollection=!mFragmented;
}
