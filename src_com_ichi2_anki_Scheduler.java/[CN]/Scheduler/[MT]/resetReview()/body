{
  String sql=String.format(Utils.ENGLISH_LOCALE,"SELECT count(*) FROM cards c WHERE queue = 1 AND due < %f",mDayCutoff);
  mRevCount=(int)mDb.queryScalar(cardLimit("revActive","revInactive",sql));
  mRevQueue.clear();
}
