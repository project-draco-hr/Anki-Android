{
  String searchText=mRestrictOnDeck + mSearchTerms;
  if (colOpen()) {
    getCards().clear();
    DeckTask.launchDeckTask(DeckTask.TASK_TYPE_SEARCH_CARDS,mSearchCardsHandler,new DeckTask.TaskData(new Object[]{getCol(),mDeckNames,searchText,((mOrder == CARD_ORDER_NONE) ? false : true)}));
  }
}
