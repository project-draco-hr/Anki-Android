{
  Deck deck=params[0].getDeck();
  if (deck != null) {
    deck.commitToDB();
    deck.updateCutoff();
    if (AnkiDroidApp.deck().hasFinishScheduler()) {
      AnkiDroidApp.deck().finishScheduler();
    }
    deck.reset();
  }
  return null;
}
