{
  super.onResume();
  if (colOpen() && !mFragmented) {
    if (Utils.now() > getCol().getSched().getDayCutoff()) {
      resetAndRefreshInterface(false);
    }
 else {
      refreshInterface();
    }
  }
 else {
  }
}
