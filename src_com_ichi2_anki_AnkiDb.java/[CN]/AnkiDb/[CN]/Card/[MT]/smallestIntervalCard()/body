{
  Card card=oneFromCursor(AnkiDb.mDb.rawQuery("select " + COLUMNS + " from "+ TABLE+ " order by /*interval*/ random() limit 1",null));
  Log.d("db","Selected card id " + card.id + " with interval "+ card.interval);
  return card;
}
