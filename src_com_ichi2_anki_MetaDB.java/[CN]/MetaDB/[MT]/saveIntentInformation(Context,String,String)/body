{
  openDBIfClosed(context);
  try {
    mMetaDb.execSQL("INSERT INTO intentInformation (source, target) " + " VALUES (?, ?);",new Object[]{source,target});
  }
 catch (  Exception e) {
    Log.e("Error","Error storing intentInformation in MetaDB ",e);
  }
}
