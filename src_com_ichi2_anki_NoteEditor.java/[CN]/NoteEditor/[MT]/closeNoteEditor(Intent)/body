{
  int result;
  if (mChanged) {
    result=RESULT_OK;
  }
 else {
    result=RESULT_CANCELED;
  }
  if (mRescheduled) {
    if (intent == null) {
      intent=new Intent();
    }
    intent.putExtra("rescheduled",true);
  }
  closeNoteEditor(result,intent);
}
