{
  double now=Utils.now();
  ContentValues updateValues=new ContentValues();
  updateValues.put("modified",now);
  updateValues.put("tags",mTags);
  updateValues.put("cache",mCache);
  AnkiDatabaseManager.getDatabase(mDeck.getDeckPath()).update(mDeck,"facts",updateValues,"id = ?",new String[]{"" + mId});
  for (  Field f : mFields) {
    updateValues=new ContentValues();
    updateValues.put("value",f.mValue);
    AnkiDatabaseManager.getDatabase(mDeck.getDeckPath()).update(mDeck,"fields",updateValues,"id = ?",new String[]{"" + f.mFieldId});
  }
}
