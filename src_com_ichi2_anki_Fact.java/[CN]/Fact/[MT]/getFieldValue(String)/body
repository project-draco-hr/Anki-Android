{
  Iterator<Field> iter=mFields.iterator();
  while (iter.hasNext()) {
    Field f=iter.next();
    if (f.getFieldModel().getName().equals(fieldModelName)) {
      return f.getValue();
    }
  }
  return null;
}
