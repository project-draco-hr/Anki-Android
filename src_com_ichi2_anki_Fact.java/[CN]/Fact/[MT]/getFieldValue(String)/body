{
  Iterator<Field> iter=mFields.iterator();
  while (iter.hasNext()) {
    Field f=iter.next();
    if (f.mFieldModel.getName().equals(fieldModelName)) {
      return f.mValue;
    }
  }
  return null;
}
