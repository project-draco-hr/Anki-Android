{
  for (  String s : new String[]{"nook"}) {
    if (android.os.Build.DEVICE.toLowerCase().contains(s) || android.os.Build.MODEL.toLowerCase().contains(s)) {
      return true;
    }
  }
  return false;
}
