{
  if (location == null) {
    if (!new File("/sdcard").exists()) {
      mCurrent=new File("/sdcard");
    }
 else {
      mCurrent=new File("/");
    }
  }
 else {
    mCurrent=new File(location);
  }
  initEntries();
}
