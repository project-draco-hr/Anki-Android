{
  double t=Utils.now();
  mDeck.setModified(Math.min(mDeck.getModified(),mServer.getTimestamp()));
  mDeck.commitToDB();
  mDeck.closeDeck();
  if (mLocalTime > mRemoteTime) {
    return "fromLocal";
  }
 else {
    return "fromServer";
  }
}
