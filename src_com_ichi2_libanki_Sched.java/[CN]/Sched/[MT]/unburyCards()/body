{
  try {
    mCol.getConf().put("lastUnburied",mToday);
  }
 catch (  JSONException e) {
    throw new RuntimeException(e);
  }
  mCol.getDb().execute("update cards set mod=?,usn=?,queue=type where queue = -2",new Object[]{Utils.intNow(),mCol.usn()});
}
