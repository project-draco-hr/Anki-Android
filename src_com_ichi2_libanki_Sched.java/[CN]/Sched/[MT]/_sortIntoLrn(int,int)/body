{
  Iterator i=mLrnQueue.listIterator();
  int idx=0;
  while (i.hasNext()) {
    if (((int[])i.next())[0] > due) {
      break;
    }
 else {
      idx++;
    }
  }
  mLrnQueue.add(idx,new int[]{due,id});
}
